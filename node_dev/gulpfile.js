const gulp = require('gulp');

gulp.task('licenses', function(done) {
    // Your license-related tasks here
    // Ensure that you properly signal when the task is complete

    // For example, if you're using async tasks, you might have something like this:
    // asyncLicenseTask()
    //     .then(() => {
    //         done();
    //     })
    //     .catch(error => {
    //         done(error);
    //     });

    // If your tasks are synchronous, you can simply call `done()` when done:
    // done();
});

